.header-container.container
  .col-sm-6
    %h2
      %i.fa-sharp.fa-solid.fa-house> Funny Movies
  .col-sm-6
    - if user_signed_in?
      = render 'shared/user_tab'
    - else
      = render 'shared/guest_tab'

